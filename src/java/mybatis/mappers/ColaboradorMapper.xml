<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="colaborador">
    
    <select id="obtenerColaborador" resultType="pojo.Colaborador">
    SELECT  c.IdColaborador,c.Nombre,c.ApellidoPaterno,c.ApellidoMaterno,c.curp,c.Correo,c.Password,c.IdRol,r.DescripcionRol AS 'Rol',c.Fotografia ,c.NoPersonal,cd.NumeroLicencia as 'numeroLicencia'
    FROM  colaborador c
    INNER JOIN Rol r ON r.IdRol = c.IdRol
    left JOIN conductor cd ON c.IdColaborador = cd.IdColaborador 
    </select>

    <select id="obtenerColaboradorNombre"  resultType="pojo.Colaborador">
    SELECT 
        c.IdColaborador,
        c.Nombre,
        c.ApellidoPaterno,
        c.ApellidoMaterno,
        c.curp,
        c.Correo,
        c.Password,
        c.IdRol,
        r.DescripcionRol AS 'Rol',
        c.Fotografia,
        c.NoPersonal,
        cd.NumeroLicencia
    FROM 
        colaborador c
    INNER JOIN 
        Rol r ON r.IdRol = c.IdRol
    LEFT JOIN 
        conductor cd ON c.IdColaborador = cd.IdColaborador
    WHERE 
        c.Nombre LIKE CONCAT('%', #{Nombre}, '%') 
    </select>

    <select id="obtenerColaboradorNoPersonal"  resultType="pojo.Colaborador">
    SELECT  c.IdColaborador,c.Nombre,c.ApellidoPaterno,c.ApellidoMaterno,c.curp,c.Correo,c.Password,c.IdRol,r.DescripcionRol AS 'Rol',c.Foto,c.NoPersonal
    FROM  colaborador c
    INNER JOIN Rol r ON r.IdRol = c.IdRol
    WHERE c.NoPersonal = #{NoPersonal};
    </select>
    
    <select id="obtenerColaboradorRol"  resultType="pojo.Colaborador">
    SELECT  c.IdColaborador,c.Nombre,c.ApellidoPaterno,c.ApellidoMaterno,c.curp,c.Correo,c.Password,c.IdRol,r.DescripcionRol AS 'Rol',c.Foto,c.NoPersonal
    FROM  Colaborador c
    INNER JOIN Rol r ON r.IdRol = c.IdRol
    WHERE r.DescripcionRol = #{Rol};
    </select>

    <insert id="registrarColaborador" parameterType="map">
        CALL RegistrarColaborador(
            #{nombre},
            #{apellidoPaterno},
            #{apellidoMaterno},
            #{curp},
            #{correo},
            #{password},
            #{idRol},
            #{noPersonal},
            #{numeroLicencia}
        )
    </insert>

    
    
    <update id="editarColaborador" parameterType="map">
    CALL actualizarColaborador(
        #{idColaborador},
        #{nombre},
        #{apellidoPaterno},
        #{apellidoMaterno},
        #{curp},
        #{noPersonal},
        #{correo},
        #{password},
        #{idRol},
        #{numeroLicencia}
    )
</update>


     <insert id="actualizarConductor" parameterType="pojo.Colaborador" >
     INSERT INTO Conductor (IdColaborador,NumeroLicencia)
     VALUES (#{IdColaborador}, #{NumeroLicencia});
    </insert>
    
    
    <delete id="eliminarColaboradorId" parameterType="pojo.Colaborador">
        DELETE
        FROM Colaborador
        WHERE IdColaborador = #{IdColaborador}
    </delete>
    
    <update id="guardarFoto" parameterType="map"> 
    UPDATE colaborador
    SET Foto = #{Foto}
    WHERE IdColaborador = #{IdColaborador};
    </update>
     
    <select id="obtenerFoto" parameterType="int" resultType="pojo.Colaborador">
    SELECT IdColaborador, c.Nombre, c.ApellidoPaterno, c.ApellidoMaterno, 
           TO_BASE64(Foto) as fotoBase64
    FROM colaborador c
    WHERE c.IdColaborador = #{IdColaborador};
    </select>

    
</mapper>